name: TDS GA7 Release Gate

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: TDS identity: 24f2007074@ds.study.iitm.ac.in
        run: echo "Identity: 24f2007074@ds.study.iitm.ac.in"
