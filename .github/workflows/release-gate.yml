name: TDS GA7 Release Gate

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 'TDS identity: 24f2007074@ds.study.iitm.ac.in'
        run: echo 'Identity verified: 24f2007074@ds.study.iitm.ac.in'
